# Phase 3 — Recipe Invocation
```text
Implement only PHASE 3 from tasks.md.
Use the provided sample recipes as immutable deterministic execution units.
Requirements:
- create recipe registry
- use Guidance / Outlines to constrain recipe proposals to registered names
- reject unknown recipes
- capture immutable execution log with inputs, outputs, errors
Avoid dynamic execution and avoid modifying recipe logic.
Add tests for successful and rejected invocations.
```
